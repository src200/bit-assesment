import React from 'react';
import { Button } from './button';

export const BasicButton = () => (
  <Button onClick={void 0}>Basic</Button>
);

export const PrimaryButton = () => (
  <Button primary onClick={void 0}>Primary</Button>
);
